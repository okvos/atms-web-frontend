/**
 * Profile Summary
 * Contains all the info needed to show the author of a comment, reply, post, etc
 */
export type ProfileSummary = {
  user_id: number;
  username: string;
  header_image_url: string;
};
