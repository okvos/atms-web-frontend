// define a navigation URL with a `label`
// and either `path` (if local in React), or `url` (if outside of React)

export const NAV_ACCENT_COLOR = "#4e98ec";
export const NAV_URLS = [

  {
    "label": "Community",
    "path": "/community"
  },
  {
    "label": "About",
    "path": "/about"
  }

];
